#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define pb push_back
const int maxN = 5e3 + 5;
int dp[maxN][maxN];

void solve()
{
    string s1, s2;
    cin >> s1 >> s2;
    int n = s1.size(), m = s2.size();
    memset(dp, -1, sizeof(dp));
    dp[0][0] = 0;
    0 
    
    
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    solve();
    return 0;
}